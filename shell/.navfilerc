#!/usr/bin/env bash

export PROJECT_HOME=$HOME/Code
alias g2code='cd $PROJECT_HOME'
alias g2="cd $PROJECT_HOME && cd"
alias g2notes="cd ~/Documents/notes/"
alias g2venvs='cd ~/.virtualenvs'
alias grepr='grep -R'
alias ackr='ack -R'
alias vimf='vim $(fzf)'
alias lsize='du -sh ./* '
alias xclip='xclip -sel c'
